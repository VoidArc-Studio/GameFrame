[package]
name = "gameframe"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
ash = "0.37"  # For Vulkan
# Add other dependencies as needed

[lib]
name = "gameframe"
path = "src/rust/lib.rs"
