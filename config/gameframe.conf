[General]
environment=steam-gamepadui
resolution=1920x1080
fullscreen=true

[Rendering]
backend=vulkan
vsync=true
max_fps=144

[GPU]
vendor=auto  # Options: nvidia, amd, intel, auto
opengl_version=2.1  # For older GPUs
vulkan_version=1.3  # For modern GPUs
